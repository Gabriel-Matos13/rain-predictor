fastapi
uvicorn[standard]
pydantic
# Pinned to the versions the model was trained/pickled with, so the deployed
# image unpickles model/model.pkl without version-mismatch warnings or errors.
scikit-learn==1.9.0
joblib==1.5.3
numpy==2.5.0
scipy==1.18.0
pandas==3.0.3
requests
